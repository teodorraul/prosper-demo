import { style } from '@vanilla-extract/css';

export const AESCNodeNodeNameStyle = style({
	fontFamily: 'monospace',
	fontWeight: 'bold',
	letterSpacing: '-0.2px',
});

export const AESCNodeConditionStyle = style({
	minHeight: 80,
	marginTop: 10,
});
